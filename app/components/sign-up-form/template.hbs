<div class='sign-up-template'>
  {{email-input email=credentials.email}}
  {{username-input username=credentials.username}}
      {{!-- ^ probably incorrect ^ --}}
  {{password-input password=credentials.password}}
  {{password-confirmation-input password=credentials.passwordConfirmation}}

  <button type="submit" class="btn btn-primary sign-up-button" {{action "submit"}}>
    Sign Up
  </button>
</div>
